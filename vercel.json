{
  "version": 2,
  "installCommand": "npm install --production=false && python -m pip install -r requirements.txt",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "main.py"
    }
  ]
}